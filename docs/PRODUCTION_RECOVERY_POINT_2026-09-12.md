# نقطة الرجوع قبل ترحيل موقع الصيانة

تاريخ فحص الواجهة: 2026-09-12؛ تصدير قاعدة البيانات واختبار الاستعادة: 2026-09-13. **نقطة الرجوع والنسخة المشفرة موجودتان؛ اكتمل ترحيل قاعدة البيانات ونشر الواجهة في 2026-09-13.**

## واجهة Vercel الحالية

- المشروع: `maintenance-website` ضمن فريق `nsq2`، الخطة Hobby.
- Deployment السابق: `dpl_5uAvdqki3qDcYUzyS1qaf7NkwD4G`، حالته `READY`، وظهر كمرشح للرجوع.
- رابط النسخة الثابت: https://maintenance-website-bo5oitzgp-nsq2.vercel.app
- النطاقان الموجهان إليها وقت الفحص السابق: https://maintenance-website-alpha.vercel.app و https://maintenance-website-nsq2.vercel.app
- المصدر: `nsqdes-source/maintenance-website`، الفرع `feature/admin-console`، الالتزام `ac0ed657dfe4d2a30faa3b5249409d8a7da14eaf`.
- وقت إنشاء النسخة: 2026-09-11 17:44:20 UTC.
- لم يُنفذ رجوع فعلي. وفق وثائق Vercel، تتيح خطة Hobby الرجوع الفوري إلى النسخة السابقة مباشرة فقط. بعد نشر النسخة الجديدة، ينبغي عدم نشر نسخة ثالثة قبل اختبار إمكان الرجوع أو تحديد آلية ترقية/إعادة نشر النسخة المرجعية.

## قاعدة Supabase الإنتاجية

- المشروع: `maintenance-website`، المعرّف `wtmzvznsmitqmjgqwtnu`، والخطة Free.
- لا توفر خطة Free نسخًا يومية مُدارة؛ تتطلب نسخة منطقية يدوية باستخدام `supabase db dump` أو `pg_dump` وفق توثيق Supabase.
- **الحالة: تم إنشاء خمسة ملفات SQL خارج المستودع في `C:\Users\Yasser-HP\Documents\maintenance-backup-20260912-1705Z`.** لا توجد نسخ مُدارة في لوحة Free.
- شملت النسخة الأدوار، مخطط التطبيق، بيانات `auth` و`public` و`storage`، ومخطط وبيانات `supabase_migrations`. الملفّات تحتوي بيانات حساسة؛ لا ترفعها إلى Git أو ترسلها في المحادثة.
- تحقق بنيوي: `data.sql` يحتوي 32 جدول `COPY`؛ الأعداد: 11 مستخدمًا في `auth.users`، 11 ملفًا شخصيًا، 30 طلبًا، 27 تكليفًا؛ `history_data.sql` يحتوي 26 ترحيلًا.
- اختبار استعادة: أُنشئت بيئة Supabase مستقلة مؤقتة على المنفذ `55322`، واستعيد `schema.sql` و`data.sql` وملفا التاريخ بنجاح. نتائج الاستعلام بعد الاستعادة: 11 مستخدمًا، 11 هوية، 11 ملفًا شخصيًا، 30 طلبًا، 27 تكليفًا، 26 ترحيلًا، صفر ملفات Storage، وصفر ملف شخصي يتيم. أُوقفت بيئة الاختبار وحُذفت وحدة بياناتها؛ المشروع المحلي الأصلي بقي يعمل.
- قيد الاستعادة: فشل سطر `GRANT SET ON PARAMETER log_min_messages TO supabase_realtime_admin` من `roles.sql` في بيئة الاختبار بسبب صلاحية النظام. نجحت أوامر `ALTER ROLE` السابقة عليه، لكن **لم يُتحقق من تطبيق ملف الأدوار كاملًا**؛ عند استعادة فعلية على مشروع Supabase جديد يجب مراجعة هذا السطر والصلاحيات وفق البيئة الهدف. لم تُنفذ أي استعادة على الإنتاج.
- بصمات SHA-256 (للتحقق من عدم تغير الملفات): `roles.sql` = `168A95A9C745AF5ED4679751F90419AC9DC434240A213B03E32A06D5664C2308`؛ `schema.sql` = `C2E5D26ACEAE66D3D176B242B5471EAA2BDAA825034C46C6D303B20858CC6AC1`؛ `data.sql` = `D94D61A0DDCF59C24832DB04501280C77CC72C47D37A64849E95493761078EFC`؛ `history_schema.sql` = `AE56295C7E66A8B46AB50DF6F00CF57F7866F2478A17FBE3910D9DEF39E836AB`؛ `history_data.sql` = `29CB4278A14A710DF8FCEE5F00D61275E52C5C11CB154C0540C5C6F15FE40A15`.
- أُنشئ الأرشيف المشفر `maintenance-backup-20260913.7z` من ملفات SQL الخمسة، ونُسخ إلى `F:\تطبيق صيانة`. حجم النسختين 23,584 بايت، وبصمة SHA-256 المتطابقة لهما: `AFDC35CA4AACDCCA496891AC915842F16E097CE52158A3C591DF28A3BC0D419E`. أظهر اختبار 7-Zip للنسخة المنسوخة 5 ملفات و«لا يوجد أخطاء» وفق صورة نتيجة الفحص التي قدمها صاحب المشروع. احتفظ بكلمة المرور منفصلة عن الأرشيف؛ لم تُسجل هنا.
## Supabase Storage

- وقت الفحص: 2026-09-12 16:46 UTC.
- عدد buckets: صفر؛ عدد objects: صفر. لا توجد ملفات حالية تحتاج نسخة مستقلة.
- أعد فحص العدد قبل الترحيل؛ إن ظهرت ملفات، انسخ كائنات Storage نفسها لأن نسخة قاعدة البيانات تحتفظ ببياناتها الوصفية فقط.

مصادر: https://supabase.com/docs/guides/platform/backups و https://vercel.com/docs/instant-rollback

## نسخة حديثة قبل الترحيل (2026-09-13 11:54 UTC)

- أُنشئت ملفات SQL الخمسة في `C:\Users\Yasser-HP\Documents\maintenance-backup-20260913-1154Z`. تحقق الفحص البنيوي من 12 مستخدمًا و12 ملفًا شخصيًا و30 طلبًا و27 تكليفًا و26 ترحيلًا وصفر ملفات Storage.
- أُنشئ الأرشيف المشفر `maintenance-backup-20260913-1154Z.7z` ونسخته المحلية والنسخة في `F:\تطبيق صيانة` متطابقتان: 24,176 بايت، SHA-256: `DF33BD0C29E8677BE5F07AC6CA1115226EF6AD09A961EDD748F9686E3BB71723`. أظهر اختبار 7-Zip على النسخة الخارجية 5 ملفات و«لا يوجد أخطاء» وفق صورة صاحب المشروع.
- هذه النسخة أحدث من النسخة السابقة، لكنها تمثل لحظة تصديرها فقط. تأكد من عدم تغير بيانات الإنتاج بعد ذلك مباشرة قبل الترحيل؛ إذا تغيرت فأعد التصدير.


## نتيجة الترحيل والنشر (2026-09-13)

- دُفعت الترحيلات الإحدى عشرة بنجاح إلى Supabase الإنتاجية؛ صار مجموع سجل الترحيلات 37، وآخرها `20260912143951_finalize_closed_request_assignments.sql`. بقيت أعداد البيانات 12 مستخدمًا و12 ملفًا شخصيًا و30 طلبًا و27 تكليفًا، ولا توجد كائنات Storage.
- نُشر الالتزام `e7305d5a32b372452ea1e371867cbc349ddcde7a` من فرع `feature/admin-console`. Deployment الجديد: `dpl_FZD6WV72TXeZZS4HQcAy6Ks2wJAG`، حالته `READY`، رابطه الثابت https://maintenance-website-nhujkob2h-nsq2.vercel.app. النطاقان العامان `maintenance-website-alpha.vercel.app` و`maintenance-website-nsq2.vercel.app` موجهان إليه دون خطأ alias.
- فُتحت صفحات الرئيسية والخدمات وطلب الخدمة وتسجيل دخول المستخدم والإدارة على النطاق المنشور. لم تُظهر سجلات Vercel أخطاء تشغيل في ساعة الفحص.
- صفحة الطلب حمّلت عنصر الخريطة لكن Google عرضت تنبيه «يتعذّر على هذه الصفحة تحميل خرائط Google بشكل صحيح»؛ إدخال الإحداثيات اليدوي متاح. يلزم متابعة فوترة/قيود مفتاح الخرائط ثم اختبارها.
- لم يُختبر تسجيل الدخول الفعلي لحساب الإدارة أو Google OAuth في هذه الجولة لعدم توفر بيانات الدخول في الجلسة. لا تُجرّب على الإنتاج ببيانات مفترضة.
- أظهر فاحص Supabase الأمني تحذيرات عن قابلية استدعاء بعض دوال `SECURITY DEFINER` من دور `anon`، وتحذيرًا عن تعطيل حماية كلمات المرور المسربة. يجب مراجعة صلاحيات الدوال بحسب الاستخدام الفعلي؛ لم تُغيّر تلقائيًا بعد النشر.
- للرجوع عن الواجهة، استخدم Deployment السابق `dpl_5uAvdqki3qDcYUzyS1qaf7NkwD4G`. الرجوع عن قاعدة البيانات إجراء مستقل يبدأ من الأرشيف المشفر الحديث أعلاه بعد تقييم البيانات التي أُنشئت منذ النسخة، ولا يُنفذ تلقائيًا.

## إعداد Google Login (2026-09-13)

- Google Cloud project: `project-062bfb8a-32c4-4d4d-adc`، عميل OAuth من نوع Web application باسم `maintenance-website production`. استخدم نطاق الموقع العام كـ JavaScript origin ورابط Supabase `https://wtmzvznsmitqmjgqwtnu.supabase.co/auth/v1/callback` كـ redirect URI. **سر OAuth محفوظ في إعدادات Google/Supabase فقط، ولا يُكتب في المستودع أو هذه الوثيقة.**
- فُعّل مزود Google في Supabase الإنتاجية. رابط العودة للموقع ضمن قائمة Redirect URLs. أُضيفت صلاحيات `openid` والبريد والملف الشخصي الأساسية في Google Auth Platform.
- جُرّب الدخول بحساب Google الموجود `nsq.des@gmail.com`؛ نجح تبادل الرمز ووصل إلى الموقع. ظل عدد المستخدمين والملفات الشخصية 12، وأضيفت هوية Google واحدة إلى حساب موجود.
- نُشرت صفحة سياسة الخصوصية المعتمدة على https://maintenance-website-alpha.vercel.app/privacy مع رابط في تذييل الصفحة الرئيسية. أُضيف الرابط إلى Google Branding، وصارت حالة جمهور Google `In production` بدل `Testing`.
- نُشر تحسين توجيه الدور في الالتزام `0df60932b2fa4149d9da84227c333d07bc5a2ca9`، Deployment `dpl_FJmuFoMNZMp3rTVAA8UnusF24AxX`، الرابط الثابت https://maintenance-website-hqophf7lg-nsq2.vercel.app. حالته READY والنطاق العام يشير إليه. عند إعادة اختبار Google انتقل حساب الفني مباشرة إلى `/technician`، ولم تظهر أخطاء تشغيل في Vercel.
- نسخة الواجهة السابقة القابلة للرجوع الآن هي Deployment `dpl_FZD6WV72TXeZZS4HQcAy6Ks2wJAG` (الالتزام `e7305d5`).

## حساب Resend وSMTP (2026-09-13)

- أُنشئ حساب Resend باسم `nsq.des` باستخدام `nsq.des@gmail.com` على خطة Transactional Free. لوحة Usage تعرض حد 3,000 رسالة شهريًا و100 رسالة يوميًا، والاستخدام الحالي 0. لم تُنشأ مفاتيح API ولم تُسجل وسيلة دفع.
- قائمة Domains في Resend فارغة. أكد صاحب المشروع أنه لا يملك نطاقًا حاليًا يمكن تعديل DNS له. نطاق الاختبار `resend.dev` لا يرسل إلا إلى بريد حساب Resend؛ لذلك لا يصلح لبريد تأكيد الحسابات أو إعادة تعيين كلمات المرور للعملاء.
- بقي إعداد `Enable custom SMTP` في Supabase الإنتاجية معطلًا كما كان، ولم تُغيّر حدود البريد. لا تفعّل SMTP حتى يتوفر نطاق مملوك وتُثبت سجلات SPF وDKIM في Resend، ثم اضبط عنوان مرسل على النطاق واختبر رسالة تأكيد واستعادة فعلية قبل رفع حد Supabase.
- مراجع التشغيل: https://resend.com/docs/send-with-supabase-smtp و https://resend.com/docs/knowledge-base/403-error-resend-dev-domain و https://supabase.com/docs/guides/auth/auth-smtp
