# ترحيلات محلية مؤرشفة

هذه النسخ محفوظة للمراجعة فقط ولا ينبغي إرجاعها إلى `supabase/migrations`.

- `20260909000300_allow_users_to_update_own_profile.sql`: يتداخل مع ترحيلات الإنتاج الأصلية `20260909171657` و`20260909171728` و`20260909172518`. إعادة تطبيق إنشاء السياسة على الإنتاج تفشل لأنها موجودة بالفعل.
- `20260909190000_add_service_request_location.sql`: يتداخل مع ترحيل الإنتاج الأصلي `20260909182430` لإضافة إحداثيات الطلب.

استُعيدت ملفات ترحيلات الإنتاج الأصلية من سجل `supabase_migrations.schema_migrations` في 2026-09-13. لا تُطبّق ملفات هذا المجلد على أي قاعدة.
